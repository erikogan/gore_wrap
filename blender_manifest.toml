schema_version = "1.0.0"

id = "gore_wrap"
version = "0.2.0"
name = "Gore Wrap"
tagline = "Flatten scanned meshes into vinyl gore strips and export SVG"
maintainer = "Erik <claude@erik.ogan.net>"
type = "add-on"

website = "https://github.com/"

tags = ["Mesh", "Import-Export", "3D View"]

blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

# numpy ships with Blender, so no bundled wheels are required.
platforms = ["macos-arm64", "macos-x64", "windows-x64", "linux-x64"]
